## 1. 测试基础设施准备

- [x] 1.1 盘点当前 `web` 测试命令与真实后端依赖点，输出待迁移清单（按模块分组）。
- [x] 1.2 引入并配置 API mock 所需依赖（`msw`）。
- [x] 1.3 建立统一测试入口与全局 setup（测试环境默认启用 mock）。

## 2. API Mock 机制落地

- [x] 2.1 建立 `handlers + fixtures` 目录规范，并按业务域拆分 mock handler。
- [x] 2.2 在 Vitest 测试生命周期中接入 `MSW` Node server，并启用 unhandled request fail-fast。
- [x] 2.3 实现场景化响应切换能力（成功、空数据、超时、服务错误）及测试辅助函数。
- [x] 2.4 为至少 2 个核心业务域提供可复用 fixture 工厂，减少测试重复数据定义。

## 3. 存量测试迁移与增强

- [x] 3.1 将 2-3 条关键业务路径测试迁移为 mock 驱动，移除真实后端依赖。
- [x] 3.2 为每条关键路径补齐异常分支断言（如 5xx、超时、空态）。
- [x] 3.3 新增规则：新增单元/组件组合测试默认禁止真实网络请求。

## 4. CI 与研发流程集成

- [x] 4.1 在 CI 中执行 mock 驱动测试流程（`npm run test:ci`）。
- [x] 4.2 输出测试分层与 mock 编写文档，包含目录规范、场景命名、常见陷阱。
- [x] 4.3 更新 PR checklist，要求功能改动附带对应 mock 测试覆盖证明。
